"use client";import {useEffect,useState} from "react";import {api,workspace} from "../../../lib/api";
export default function Schedules(){const[s,setS]=useState<any[]>([]);useEffect(()=>{workspace().then(w=>w&&api(`/api/v1/schedules?workspaceId=${w.id}`)).then(x=>x&&setS(x))},[]);return <><div className="pageHead"><div><div className="kicker">Background execution</div><h1>Schedules</h1></div></div><div className="card"><table className="table"><tbody>{s.map(x=><tr key={x.id}><td><b>{x.name}</b></td><td>{x.agent.name}</td><td><code>{x.cronExpression}</code></td><td>{x.timezone}</td></tr>)}</tbody></table>{!s.length&&<div className="empty">Create schedules from the API or agent configuration.</div>}</div></>}
