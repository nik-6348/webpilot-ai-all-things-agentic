{
  "name": "@webpilot/browser-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc -p tsconfig.json && node --watch dist/main.js",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@webpilot/agents": "workspace:*",
    "@webpilot/contracts": "workspace:*",
    "@webpilot/database": "workspace:*",
    "@webpilot/gcp": "workspace:*",
    "@webpilot/observability": "workspace:*",
    "@webpilot/security": "workspace:*",
    "@webpilot/workflow-engine": "workspace:*",
    "fastify": "5.6.0",
    "playwright": "1.55.0",
    "zod": "4.1.5"
  },
  "devDependencies": {
    "tsx": "4.20.5"
  }
}
